
#stack manipulation utilities

label stack::swap
    pull address

    pull stack::a
    pull stack::b
    push stack::a
    push stack::b

    push address
    return


label stack::dup
    pull address

    pull stack::a
    push stack::a
    push stack::a

    push address
    return

label stack::drop
    pull address

    pull stack::a
    
    push address
    return
