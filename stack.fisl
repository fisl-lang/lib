
#stack manipulation utilities

label stack::swap
    pull address

    pull a
    pull b
    push a
    push b

    push address
    return


label stack::dup
    pull address

    pull a
    push a
    push a

    push address
    return

label stack::drop
    pull address

    pull a
    
    push address
    return
